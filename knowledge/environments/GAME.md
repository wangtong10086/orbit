# GAME Environment

## Key Facts
- Scheduling weight 3.0 (sampled 3x more often by validators — more data points, NOT 3x scoring weight)
- Scoring uses geometric mean across all environments equally — no per-env weight multiplier
- Multi-turn gameplay, assistant replies are often single digits (action IDs)
- Eval uses OpenSpiel framework with 22+ game types
- `strip_think_tags=True` in eval — think blocks are auto-stripped
- 2 retry mechanism: model gets a second chance on parse errors
- Scoring: win/loss/draw per game, averaged across samples
- Leaderboard top scores: ~45-65 points

## Format Requirements
- System prompt tells model to respond with action ID
- Assistant reply: pure integer (action ID) or think block + integer
- Eval parser extracts the number; anything else = parse error

## Data Issues (Resolved)
- **v5 CoT conflict (29% parse error)**: 54.4% of data had `<think>` tags, 45.6% didn't. System prompts contradicted — some said "ONLY the action ID" but assistant had think tags. Fixed in v7 by unifying system prompt to CoT version.
- **DDB dirty entries**: 20 entries with format contamination (`.3`, `".6`, long text residuals). Cleaned in v7.
- **CoT truncated tags**: 35 entries with incomplete think blocks. Cleaned in v7.
- **38 duplicates**: Deduplicated in v4.

## Per-Game Learnability (v7-v10 evaluation)
| Tier | Games | Notes |
|------|-------|-------|
| Solved | goofspiel | 100% win rate consistently |
| Strong | leduc_poker, bridge, blackjack, euchre | Strategy-learnable via SFT |
| Improved by bot data | gin_rummy, hearts | v7→v8: gin_rummy 0%→100% after bot strategy data added |
| Zero (needs better strategy) | othello, hex, liars_dice | Always 0%, may need specialized bot data |
| Unlearnable by LLM | go, chess, checkers, solitaire | Deep search games, SFT cannot teach |

## Bot Strategy Data (v8+)
- 2193 programmatic strategy entries covering 7 games
- Generated by game-specific bots, not LLM distillation
- Proved effective: gin_rummy broke through 0% immediately after inclusion

## Training Data Mix (latest)
- v10: 4610 entries (2417 clean DDB/CoT + 2193 bot), 34.7% of total mix
- v8 (GAME-focused): 4610 entries, 65.7% share

## Evaluation Setup
- Must use `--concurrency 4 --timeout 7200` (old 600s timeout missed long games)
- Only eval games present in training data (22 games exist, ~15 trainable)
- v9 mean jumped 0.10→0.19 just from timeout fix

## Current Best / Status
- v11: mean=0.226, 39% non-zero rate, 100 samples (~22.6 leaderboard points)
- Leaderboard top miners: ~45-65 points
- Parse error reduced from 29% (v5) → 12% (v7) → ~0% (v9+)
- Best games: leduc_poker (full win), goofspiel (100%), euchre (good)
- Worst: othello, hex, go, checkers (0% — need search, not SFT-learnable)

## Improvement Directions
- More bot strategy data for othello/hex/liars_dice
- Increase sample count for evaluation reliability
- Consider game-specific fine-tuning for high-weight games
- RL/DPO on game outcomes (DPO pipeline already built, 589 GAME pairs)
