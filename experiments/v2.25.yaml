# v2.25 — Current canonical data
# STATUS: approved
# Created: 2026-03-26 10:00 UTC

version: v2.25
status: completed

variable: "GAME v10 9966 (rule-based think + gin_rummy 1000) + NW 4148 + LW 8816 + SWE-I 853"
hypothesis: >
  GAME v10: rule-based IF-THEN think chains (replaces MCTS stats), 13 config fixes,
  gin_rummy 1000, multi-difficulty mix (47% random + 27% weak MCTS + 26% medium MCTS).
  LW 8816: tools param fix (selector not text), goto+stop by design.
  NW 4148 (17.4% — below 19% threshold, regression risk accepted).
  SWE-I 853. NO reasoning-parser. Checkpoint ~80-85%.

data_mix:
  GAME: 9966
  NAVWORLD: 4148
  LIVEWEB: 8816
  SWE-INFINITE: 853
  total: 23783

config:
  base_model: unsloth/Qwen3-32B-bnb-4bit
  method: QLoRA-SFT
  lr: 5e-5
  lora_r: 64
  lora_alpha: 128
  epochs: 1
  batch_size: 2
  grad_accum: 2
  seq_len: 8192
  packing: true
  num_gpus: auto
  ddp: true
  save_steps: 100

eval_plan:
  environments: [GAME, NAVWORLD, LIVEWEB, SWE-INFINITE]
  samples_per_env: 100
  sglang_args: "--tool-call-parser qwen"
  NO_reasoning_parser: true
  MUST_SET: "export AMAP_MAPS_API_KEY and AMAP_API_KEY"
  MUST_DO: "Merge from checkpoint ~80-85%. NO --reasoning-parser. Sanity test."

blockers: []
machine: m1
