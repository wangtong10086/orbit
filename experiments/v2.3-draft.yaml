# v2.3 DRAFT — evolves each loop while v2.2 evaluates
# STATUS: drafting (DO NOT LAUNCH — awaiting v2.2 eval results)

version: v2.3
status: drafting

analysis: >
  v2.2 eval in progress: GAME ~28.8% at 64/100 (above v2.1 25.74%).
  Major data improvements ready for v2.3:
  1. GAME v4: quality overhaul (3316→2815, all-English thinks, zero-tier downsampled)
  2. LIVEWEB: format fix + 18 new Claude trajectories (12 score=1.0)
  3. LIVEWEB: pipeline fully operational across 7 plugins

# v2.3 is now a MULTI-IMPROVEMENT version (like v2.2 was):
# - GAME data quality (v4 cleanup)
# - LIVEWEB format fix + new data
# Both are confirmed improvements, not speculative.

variable: "GAME v4 data quality + LIVEWEB format fix + new trajectories"
hypothesis: >
  GAME v4 improvements: removing 545 no-think entries, replacing Chinese/template
  thinks with diverse English reasoning, downsampling zero-tier (658→155) should
  improve GAME score from ~28% to 32-38%. The model wastes less capacity on
  unlearnable games and gets better reasoning examples.
  LIVEWEB: format fix from 0→5-12% (Qwen3 native tool_call format + system tool defs).
  18 new Claude trajectories add diversity beyond CoinGecko.

confirmed_changes:
  game_v4:
    description: "Quality overhaul: all-English thinks, diverse reasoning, zero-tier downsampled"
    entries: 2815  # was 3316 in v2.2
    key_improvements:
      - "Removed 545 no-think entries"
      - "Replaced 9062 Chinese think msgs with English"
      - "Replaced 14351 template thinks with diverse reasoning"
      - "Downsampled zero-tier 658→155 (20%→6%)"
      - "Fixed liars_dice bot dice parse bug"
      - "New v4 bot entries: leduc 192, gin_rummy 294, goofspiel 94, liars_dice 195"
  liveweb_format:
    description: "Qwen3 native <tool_call> format + <tools> system definitions"
    fix: "_normalize_tool_calls_qwen3() in prepare-data"
  liveweb_new_data:
    description: "18 Claude trajectories (12 score=1.0), all fit seq=16k"
    total: "356 + 18 = 374 entries"

data_mix:
  GAME: 2815       # v4 cleaned (was 3316 in v2.2)
  NAVWORLD: 2624   # unchanged
  SWE-SYNTH: 983   # unchanged
  LIVEWEB: 374     # 356 + 18 new, WITH correct format
  # Total: ~6796

config_draft:
  seq_len: 16384        # confirmed safe in v2.2
  batch_size: 1
  grad_accum: 4
  num_gpus: auto
  ddp: true
  lr: 1e-4
  lora_r: 64

eval_plan:
  environments: [GAME, NAVWORLD, SWE-SYNTH, LIVEWEB]
  samples_per_env: 100
  tool_call_parser: qwen25

# Will finalize and approve once v2.2 eval completes
# Key decision: if v2.2 NAVWORLD is low, add more Claude Sonnet data
