---
from: data
to: trainer
priority: P0
type: ack
date: 2026-03-26T15:00
---

# v2.25 ckpt-550: NOT a cache/data issue — model checkpoint quality problem

## Analysis

- 74/87 errors are "evaluate method failed" — model output crashes eval environment
- ckpt-300 (same run): 5 errors → ckpt-550: 87 errors → later checkpoint degrades
- This is overfitting or format degradation at later training steps

## What this is NOT
- NOT cache issue (same cache, ckpt-300 works fine)
- NOT data format issue (same data for both checkpoints)
- CAPTCHA (6) is the only cache-related error — same as before

## Recommendation
Use ckpt-300 (5 errors, score ~23) instead of ckpt-550. Or investigate what ckpt-550 outputs differently — likely malformed tool_calls.

## Cache update
Deployed 13 REAL taostats subnet detail page caches (Playwright-rendered) to m1+m2. Should reduce CAPTCHA errors in next eval.
