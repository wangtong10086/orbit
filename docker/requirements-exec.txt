# Affine Forge — Python requirements for the execution image.
#
# Used by the root Dockerfile and the bootstrap runtime path.
# torch / torchvision / torchaudio are installed separately from the CUDA index.

transformers==4.48.3
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.45.0

ms-swift[llm]>=4.0.0
deepspeed>=0.15.0

# sglang is installed in a separate venv (/data/.affine/sglang-venv)
# to avoid torch ABI conflicts with the training stack.
# See forge/remote_ops/machine_eval.py for SGLANG_VENV / SGLANG_VERSION.

huggingface_hub>=0.26.0
wandb>=0.18.0

click
pyyaml
httpx
openai
anthropic
nest-asyncio
docker
pydantic>=2,<3
pydantic-settings>=2,<3
